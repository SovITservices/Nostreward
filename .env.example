# Bot's Nostr private key (hex or nsec format)
BOT_NSEC=nsec1your_private_key_here

# Nostr Wallet Connect URL from your Lightning wallet (Alby, Mutiny, etc.)
# Only required if ENABLE_ZAP=true
# Format: nostr+walletconnect://pubkey?relay=wss://relay.example.com&secret=hex
NWC_URL=nostr+walletconnect://your_wallet_pubkey?relay=wss://relay.getalby.com/v1&secret=your_secret_hex

# Relays to connect to (comma-separated). Defaults to Primal + Damus if not set.
# RELAYS=wss://relay.primal.net,wss://relay.damus.io,wss://nos.lol

# Default zap amount in sats (default: 21)
# ZAP_AMOUNT_SATS=21

# === Action Toggles (set to "true" to enable) ===
ENABLE_ZAP=true
ENABLE_REPOST=true
ENABLE_WHITELIST=true

# Required hashtag that notes must include (without #). Default: nostreward
# Users must post notes with this hashtag for the bot to detect them.
# REQUIRED_HASHTAG=nostreward

# === File Paths (defaults shown) ===
# CODES_FILE=codes.json
# WHITELIST_FILE=whitelist.json
